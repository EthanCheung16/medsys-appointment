# Root configuration for the module.
config.stopBubbling = true

# Marks Lombok-generated methods with @lombok.Generated so that SpotBugs and JaCoCo
# ignore code the developer did not write. Without this, generated getters/setters
# distort both the static-analysis findings and the coverage ratio.
lombok.addLombokGeneratedAnnotation = true
